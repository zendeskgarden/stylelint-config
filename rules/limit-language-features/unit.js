'use strict';

module.exports = {
  'rules': {
    // Specify a blacklist of disallowed units
    'unit-blacklist': null,
    // Specify a whitelist of allowed units
    'unit-whitelist': null
  }
};
